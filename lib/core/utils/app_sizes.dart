import 'package:get/get.dart';

class AppSizes {
  // Screen width and height
  static double get width => Get.width;
  static double get height => Get.height;

  // Padding sizes
  static double paddingASmall = Get.width * 0.001;
  static double paddingSmall = Get.width * 0.02;
  static double paddingMedium = Get.width * 0.04;
  static double paddingLarge = Get.width * 0.06;
  static double paddingMLarge = Get.width * 0.8;
  static double paddingALarge = Get.width * 0.1;

  // Font sizes
  static double fontSubSmall = Get.width * 0.03;
  static double fontSmall = Get.width * 0.03;
  static double fontMedium = Get.width * 0.05;
  static double fontLarge = Get.width * 0.06;

  // Margin sizes
  static double marginSmall = Get.width * 0.02;
  static double marginMedium = Get.width * 0.04;
  static double marginLarge = Get.width * 0.06;
}
